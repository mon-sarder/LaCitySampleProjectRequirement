# =============================================================================
# Environment Configuration for Robot Driver API
# =============================================================================
#
# This is a template file. To use it:
# 1. Copy this file and rename it to `.env`
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the `.env` file to version control
#
# =============================================================================

# -----------------------------------------------------------------------------
# Claude API (Anthropic)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/settings/keys
# Your key should start with: sk-ant-api03-
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# -----------------------------------------------------------------------------
# Flask Application Configuration
# -----------------------------------------------------------------------------
# Secret key for session management (generate a random string for production)
# Example: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET=dev-secret-change-me-in-production

# API key for JSON endpoints (used by MCP bridge and external clients)
API_KEY=secret123

# Content Security Policy mode (1=relaxed for local dev, 0=strict for production)
RELAXED_CSP=1

# Auto-create default admin user (admin/admin123) on startup (1=yes, 0=no)
ADMIN_DEFAULT=1

# -----------------------------------------------------------------------------
# Robot Driver MCP Server Configuration
# -----------------------------------------------------------------------------
# Base URL for the Robot Driver API (change if running on different host/port)
ROBOT_BASE_URL=http://localhost:5001

# API key for MCP bridge to authenticate with Robot Driver API
ROBOT_API_KEY=secret123

# -----------------------------------------------------------------------------
# Optional: Custom User Agent
# -----------------------------------------------------------------------------
# Override the default user agent for Playwright requests
# CUSTOM_UA=BroncoBot/1.0 (+https://github.com/mon-sarder/BroncoFit)

# -----------------------------------------------------------------------------
# Optional: Database Configuration
# -----------------------------------------------------------------------------
# Custom path for SQLite database (defaults to backend/users.db)
# DB_PATH=/path/to/custom/users.db

# -----------------------------------------------------------------------------
# Optional: Advanced Settings
# -----------------------------------------------------------------------------
# Flask debug mode (never use in production!)
# FLASK_DEBUG=0

# Flask host binding (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
# FLASK_HOST=0.0.0.0

# Flask port
# FLASK_PORT=5001

# Maximum content length for uploads (in bytes)
# MAX_CONTENT_LENGTH=1048576

# Session lifetime (in minutes)
# SESSION_LIFETIME=30

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - Lines starting with # are comments
# - Do not use quotes around values unless they contain spaces
# - Spaces around = are ignored (KEY=value and KEY = value both work)
# - Empty values are treated as unset
# - For security, always keep your .env file out of version control